cmake_minimum_required(VERSION 3.20.0)
set(projectName "xbwuc_base" )
project(${projectName} C CXX)
set(CMAKE_CXX_STANDARD 11)
add_library(xbwuc_base OBJECT  "logger.cpp" "jnicommon.cpp")
add_library(xbwuc::base ALIAS xbwuc_base )
find_package(Poco REQUIRED COMPONENTS  Foundation)
target_include_directories(${projectName} INTERFACE ".")
target_link_libraries(${projectName} PRIVATE Poco::Foundation )
