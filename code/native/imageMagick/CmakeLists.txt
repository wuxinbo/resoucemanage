cmake_minimum_required(VERSION 3.20.0)
set(projectName "imageMagick" )
project(${projectName} C CXX)
set(CMAKE_CXX_STANDARD 11)
if (LINUX)
    add_subdirectory(linux)
    include_directories("./include/linux")
endif()
if(WIN32)
    add_library(${projectName} OBJECT  "imageMagick.cpp")
    target_link_directories(${projectName} PUBLIC "./lib")
    target_include_directories(${projectName} PUBLIC ".")
    target_link_libraries(${projectName} PUBLIC base "CORE_RL_MagickWand_" Poco::Foundation )
endif()